# Task 001: Project Initialization

**Status**: [ ] Not Started
**Roadmap Reference**: Stage 1.1 - Project Skeleton.

## Objectives
1. Initialize a Flutter project in the current directory.
2. Standardize organization and project names.
3. Establish the base folder structure for Clean Architecture.

## Steps
1. [ ] Run `flutter create . --org com.pharmafix --project-name pharma_fix`.
2. [ ] Validate `pubspec.yaml` creation.
3. [ ] Create skeletal directories:
    - `lib/core/`
    - `lib/features/`
    - `lib/data/`
    - `lib/domain/`
4. [ ] Clean up redundant boilerplate (remove extra comments from `main.dart`).

## Success Criteria
- [ ] Command `flutter doctor` runs without critical errors.
- [ ] Directory structure matches `ARCHITECTURE.md`.
- [ ] Project compiles and runs (initial state).
